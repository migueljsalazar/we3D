<%= render 'form' %>

 <canvas id="cv" width=640 height=480>
        It seems you are using an outdated browser that does not support canvas :-(
        </canvas>
        <script type="text/javascript">
            var viewer = new JSC3D.Viewer(document.getElementById('cv'));
            viewer.setParameter('SceneUrl',         'http://download1648.mediafire.com/sp618drisp1g/62lb269kwgqoaqz/oni_skull-hollow.stl');
            viewer.setParameter('ModelColor',       'B2BCC1');
            viewer.setParameter('BackgroundColor1', '#000000');
            viewer.setParameter('RenderMode',       'texturesmooth')
            viewer.setParameter('Renderer', 'webgl');
            viewer.init();
            viewer.update();
        </script>
